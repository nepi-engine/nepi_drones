# Curated, independent physical dimensions for obstacle_course/model.sdf.
# Rendered into model.sdf by sim_container/scripts/generate_model_sdf.py --
# ramp geometry (run length, box length) and baffle placement are DERIVED
# from ramp_rise_m/ramp_angle_deg/baffle_gap_m below, not stored separately,
# so they can't drift out of sync the way the original hand-authored file's
# three independent "6.0" corridor-width literals could.
#
# Edited via the Sim Connector RUI's "Environment Dimensions" controls, or by
# hand followed by `python3 generate_model_sdf.py obstacle_course`. Takes
# effect on the next simulator Launch, not live.

course_start_x_m: 2.0
corridor_width_m: 6.0
wall_length_m: 22.0
wall_thickness_m: 0.2
wall_height_m: 1.0
baffle_a_x_m: 8.0
baffle_b_x_m: 14.0
baffle_gap_m: 0.4
baffle_thickness_m: 0.2
ramp_start_x_m: 18.0
ramp_rise_m: 0.35
ramp_angle_deg: 9.97
ramp_plateau_length_m: 1.0
